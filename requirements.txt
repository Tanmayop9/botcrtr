requests>=2.28.0,<3.0.0
pyotp>=2.9.0,<3.0.0
# The two packages below are only needed for Method 2 (Browser / Selenium).
# On Termux use Method 1 (API) -- no browser is available via pkg.
selenium>=4.0.0,<5.0.0
webdriver-manager>=4.0.0,<5.0.0
